My config files
===

###git config 
---
* 1.Example:

    `git config --system user.name 柏健`

    `git config --global user.name 柏健`

    `git config --local user.name 柏健`

* 2.file location

    /etc/gitconfig

    ~/.gitconfig

    repo/.git/config

